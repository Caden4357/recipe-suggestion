import React, {useState} from 'react';
import { View, Text} from 'react-native';

const Camera = () => {
    return (
        <View>
            <Text>Camera</Text>
        </View>
)}

export default Camera;